---

import { getPinnedRepos } from '../api/pinnedGithubRepos';

interface Project {
  name: string;
  description: string;
  url: string;
}

let projects: Project[] = [];
try {
  projects = await getPinnedRepos();
} catch (error) {
  console.error('Failed to fetch pinned repositories:', error);
}
---

<section id="opensource" class="py-20 bg-light">
  <div class="container mx-auto px-4">
    <div class="text-center mb-16">
      <h2 class="text-3xl font-bold mb-4">
        Open <span class="text-transparent bg-clip-text bg-gradient-to-r from-accent to-elixir">Source</span> Projects
      </h2>
      <div class="h-1 w-24 bg-gradient-to-r from-accent to-elixir mx-auto mb-6"></div>
      <p class="max-w-2xl mx-auto text-gray-700">
        We contribute to and maintain open source projects that solve real problems in the African context.
      </p>
    </div>
    
    <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
      {projects.map(project => (
        <div class="bg-white rounded-xl p-6 shadow-md card-hover">
          <div class="flex items-center mb-4">
            <div class={`w-10 h-10 bg-secondary rounded-full flex items-center justify-center text-white`}>
              <i class="fab fa-github"></i>
            </div>
            <h3 class="text-xl font-bold ml-3">{project.name}</h3>
          </div>
          <p class="mb-4">{project.description}</p>
          <a href={project.url} class="text-primary font-medium hover:underline" target="_blank" rel="noopener noreferrer">View on GitHub →</a>
        </div>
      ))}
    </div>
    
    <div class="mt-16 bg-gradient-to-r from-accent to-elixir text-white rounded-xl p-8 max-w-4xl mx-auto">
      <div class="flex flex-col md:flex-row items-center">
        <div class="md:w-2/3 mb-6 md:mb-0">
          <h3 class="text-2xl font-bold mb-3">Contribute to Our Projects</h3>
          <p>Join our community of contributors to build open source solutions that address local challenges.</p>
        </div>
        <div class="md:w-1/3 text-center">
          <a 
            href="#" 
            class="inline-block bg-white text-accent hover:bg-gray-100 font-bold py-3 px-6 rounded-lg transition duration-300"
          >
            Get Started
          </a>
        </div>
      </div>
    </div>
  </div>
</section>
